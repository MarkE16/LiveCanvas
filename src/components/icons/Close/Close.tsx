import { CircleX as LucideCircleX } from "lucide-react";
import type { ComponentProps } from "react";

const Close = (props: ComponentProps<"svg">) => <LucideCircleX size="1em" {...props} />;

export default Close;
